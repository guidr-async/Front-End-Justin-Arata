import React from 'react';

const ProfileTrips = props => {
    return (
        <div>
            {/*User ID's trip cards mapped out that are editable if correct user*/}
            Profile Trips
        </div>
    )
};
export default ProfileTrips;